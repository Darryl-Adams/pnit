/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,ts,jsx,tsx}",  // Scan all files in /src
      "./public/index.html"           // (if you ever add static HTMLs)
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  